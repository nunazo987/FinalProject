import { LapTimeFormatPipe } from './lap-time-format-pipe';

describe('LapTimeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LapTimeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
